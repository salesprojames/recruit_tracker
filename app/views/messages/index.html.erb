<% @messages.each do |message| %>
	<%= message.body %>-<%= message.sent_by_recruit %><br>
<% end -%>

<!-- need to add messages pane -->

<%= link_to "View Conversation", new_recruit_message_path(@recruit) %>